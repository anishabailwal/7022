// 21. Write a C++ program to compute the square root of an given integer
#include<iostream>
#include<cmath>
using namespace std;

int main()
{
    int n;
    cout<<"Enter the number you want sqaure root of :";
    cin>>n;
    cout<<"The square root of "<<n<<" is "<<sqrt(n);
    return 0;
}